project Tester is

   for Languages use ("Ada");
   for Main use ("tester.adb");

   package Builder is
      for Executable ("tester.adb") use "adatftpd-tester";
   end Builder;

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use "test";

end Tester;

